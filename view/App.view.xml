<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	controllerName="pt.ewm.am.controller.App"
	displayBlock="true"
	xmlns="sap.m">
	
	<!-- Central View -->
	<App id="app"
		 busy="{appView>/busy}"
		 busyIndicatorDelay="{appView>/delay}"/>

</mvc:View>